// This file will bootstrap your React frontend inside Electron
// The frontend is already loaded in main.js via loadFile

const { ipcRenderer } = require('electron');

// The React app is loaded directly from the build folder
// No additional setup needed here
